/*---------------------------------- 头文件保护 ----------------------------------*/
/* 防止重复包含，确保头文件内容在同一个编译单元中只展开一次 */
#ifndef __USART3_H
#define __USART3_H

/*---------------------------------- 依赖包含 ----------------------------------*/
#include "sys.h"  // 包含芯片外设寄存器定义和基础驱动库

/*---------------------------------- 函数声明 ----------------------------------*/

/* 通过USART3发送字符串
 * @param *String 要发送的字符串指针
 */
void USART3_Send_String(char *String);

/* USART3初始化函数
 * @param bound 波特率设置值（单位：bps）
 */
void uart3_init(u32 bound);

/* USART3中断服务函数
 * 功能：处理USART3接收中断事件，解析数据并更新控制标志
 */
void USART3_IRQHandler(void);

#endif  // 结束头文件保护
